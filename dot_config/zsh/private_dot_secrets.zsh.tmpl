# Secrets - managed by chezmoi
# This file contains sensitive environment variables

# 1Password CLI status: {{ if .hasOp }}available{{ else }}not installed{{ end }}

# To use 1Password secrets, edit this file with 'chezmoi edit ~/.config/zsh/.secrets.zsh'
# and add lines like:
#   export OPENAI_API_KEY="$(op read 'op://Private/OpenAI API/credential')"
#
# Or set secrets manually:
#   export OPENAI_API_KEY="your-key-here"
#   export ANTHROPIC_API_KEY="your-key-here"
#   export GITHUB_TOKEN="your-token-here"

{{ if .hasOp -}}
export CLOUDFLARE_API_TOKEN="$(op read 'op://AstroCapital/Cloudflare DNS/credential')"
{{ end -}}

# Source local secrets if they exist (not managed by chezmoi)
if [[ -f "${ZDOTDIR:-$HOME/.config/zsh}/.secrets.local.zsh" ]]; then
    source "${ZDOTDIR:-$HOME/.config/zsh}/.secrets.local.zsh"
fi
